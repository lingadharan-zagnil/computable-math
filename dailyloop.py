# pseudocode

def day():
    boot(power_ok=True)
    for block in range(2):             # minimum viable deep work
        load(goal.select_top())
        end_task(noise.processes())
        if not goal.has_next_action():
            goal.slice_to_30min_step()
        focus(timer=75)
        ship(min_output=True)          # artifact > intention
        cool_down(15)
    do(maintenance: admin+chores, batch=True)
    move(45); eat(sane=True); hydrate()
    log(shipped, learned, blocked)
    garbage_collect(thoughts.useless())
    sleep()
